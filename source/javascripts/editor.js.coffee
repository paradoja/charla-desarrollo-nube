Reveal.initialize
  controls: true
  progress: true
  history: true
  rollingLinks: false
  slideNumber: true
  maxScale: 3.0

  theme: Reveal.getQueryHash().theme || 'default'
  transition: Reveal.getQueryHash().transition || 'default'

  dependencies: [
  ]
